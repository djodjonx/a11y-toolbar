import type { DisplayableContent } from "@/assets/types";
import Content from "../index";

export default class SummaryContent extends Content {
  constructor(body: DisplayableContent) {
    super(body)
  }
}